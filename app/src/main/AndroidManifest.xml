<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tridonic.irfernbedienungdali_rc" >

    <uses-sdk
        android:minSdkVersion="20"
        android:targetSdkVersion="21" />
    <uses-feature android:name="android.hardware.consumerir" />
    <uses-permission android:name="android.permission.TRANSMIT_IR" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >


        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity            android:name=".installationsmodus"            android:label="@string/app_name" >        </activity>
        <activity            android:name=".programmiermodus"            android:label="@string/app_name" >        </activity>
        <activity            android:name=".standardmodus"            android:label="@string/app_name" >        </activity>
        <activity            android:name=".UserSettingActivity"            android:label="@string/app_name" >        </activity>


    </application>

</manifest>
